{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "laravel-boost": {
      "type": "local",
      "command": ["docker", "compose", "exec", "-T", "app", "php", "artisan", "boost:mcp"],
      "enabled": true
    }
  }
}
